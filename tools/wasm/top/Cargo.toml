[package]
name = "top"
version = "1.4.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[build-dependencies]
nopeek_widgets = { path = "../sdk/widgets" }

[profile.release]
opt-level = "s"
lto = true
strip = true
