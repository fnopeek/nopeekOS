[package]
name = "wifi_ax200"
version = "0.64.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[build-dependencies]
nopeek_widgets = { path = "../sdk/widgets" }

[profile.release]
opt-level = "s"
lto = true
strip = true
