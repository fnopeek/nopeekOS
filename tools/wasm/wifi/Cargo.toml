[package]
name = "wifi"
version = "1.53.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[build-dependencies]
nopeek_widgets = { path = "../sdk/widgets" }

[profile.release]
opt-level = "s"
lto = true
strip = true
