[workspace]
members = ["kernel"]
exclude = ["tools/wasm/wallpaper", "tools/wasm/top", "tools/wasm/wifi"]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
